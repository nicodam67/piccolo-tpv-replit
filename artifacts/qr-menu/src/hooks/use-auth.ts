/**
 * Auth hooks — thin wrappers around convex/react and @convex-dev/auth/react.
 *
 * Wraps @convex-dev/auth hooks for use throughout the app.
 */
export { useConvexAuth as useAuth } from "convex/react";
export { useAuthActions as useAuthActions } from "@convex-dev/auth/react";
